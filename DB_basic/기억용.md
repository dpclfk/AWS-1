```sql
cd /usr/local/mysql/bin

sudo vi ~/zshrc

입력 export PATH="/usr/local/mysql/bin:$PATH"
source ~/.zshrc

테이블 생성

CREATE TABLE 테이블명 (
  컬럼명 타입 옵션 PK
)

타입 : VARCHAR(N) : 최대 글자수 제한, 최소제한 없음
INT, MEDIUMINT UNSIGNED : 숫자만 입력가능, UNSIGNED의경우 0부터 시작
ENUM('data1', 'data2') : 정해진 데이터중 하나만 넣을수있음
SET('data1', 'data2'): 정해진 데이터만 넣을수있음, 여러개 동시에 가능

옵션 : UNIQUE : 중복값 없음
PRIMARY KEY : 기준이 되는값, 중복값 없음
NOT NULL : 빈값 입력불가

CREATE TABLE aws_student (
  id TINYINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
  name VARCHAR(5) NOT NULL
)

CONSTRAINT : 테이블 규칙 정하기

FOREIGN KEY (현재테이블) REFERENCES 다른테이블(가져올 값)




테이블 수정
ALTER TABLE 테이블명 ADD COLUMN 컬럼명 타입 : 기존에 존재하는 테이블에 새로운 컬럼넣기
ALTER TABLE 테이블명 MODIFY COLUMN 컬럼명 타입 : 기존에 존재하는 컬럼을 수정함
ALTER TABLE 테이블명 DROP COLUMN 컬럼명 타입 : 존재하는 컬럼 삭제

데이터 입력
INSERT INTO 테이블명 (컬럼명) VALUES (데이터)
INSERT INTO board(title, writer, content) VALUES('타이틀입니다', 1, '첫글');

입력된 데이터 확인 및 찾기
SELECT * FROM 테이블명 WHERE 컬럼명

SELECT * FROM baord; -- 작성자의 id를 찾을 수 있다.
SELECT * FROM user WHERE id=1;

SELECT * FROM test;
SELECT id, nick FROM test WHERE id=2;
SELECT * FROM test WHERE nick LIKE '%닉%';
SELECT * FROM test WHERE nick LIKE '닉%' AND id > 3;
SELECT * FROM test WHERE nick LIKE '닉%' OR id > 3;
SELECT * FROM test WHERE nick LIKE '닉__';

- \%의 경우 몇글자든 상관 없다.
- \_의 경우 글자 하나를 뜻한다.

데이터 삭제
DELETE FROM 테이블명 WHERE id=1;


INDEX 만들기

CREATE INDEX idx_board_title ON board_test(title ASC)
CREATE INDEX idx_board_title ON board_test(title DESC);

- ASC : 오름차순
- DESC : 내림차순

INDEX 보기
SHOW INDEX FROM board_test;

INDEX 지우기
DROP INDEX idx_bobrd_title ON board_test;
```

npx = excute
