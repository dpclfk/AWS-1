# 소개

NestJs는 NodeJs환경에서 서버를 실행하기 위해 만들어진 프레임워크다
TypeScript언어로 작성하며 빌드(컴파일)하여 JavaScript로 변환한뒤 NodeJs환경에서 실행된다
왜 컴파일해서 실행할까? TS는 런타임(실행환경)이 따로없어, JS런타임에서 실행해야된다.
그렇다면 런타임이없는 TS는 왜 쓸까? JS는 타입오류가 있을경우(의도하지 않은 값), 타입오류가있다고 확인이 되지않고 실행된다.
하지만 TS의 경우 타입오류가 있을경우, 컴파일 단계에서 확인이 가능하다
(정확히 말하면 TS->JS는 컴파일이 아닌 트랜스파일이라고 얘기함)

또한 NestJs에서 프로그래밍을할때 OOP(객체 지향 프로그래밍), FP(함수형 프로그래밍), FRP(함수 반응형 프로그래밍)을 사용하게된다
OOP는 객체를 만들고 그 만들어진 객체를 외부에서 수정하지 못하게 한다
FP는 같은값을 받으면 같은값을 내보내며, 해당함수 외부에 영향을 끼치지 않는다.
(리턴값 말고, 단순히 함수를 실행했다고 바뀌진 않는다)
FRP는 FP랑 RP(반응형 프로그래밍)이랑 합쳐진것이다
RP는뭘까? 하나의 값을 바꾸면 해당값을 가져다쓰는 다른값들도 연쇄적으로 바뀌는것

NestJs는 Express라이브러리에서 사용하던대로 사용할 수 있다.
또는 fastify라는 라이브러리에서 사용하던대로 사용할 수 있다.

NestJs는 Express 또는 fastify 라이브러리를 사용할때보다 더 상위개념을 미리 만들어 놓을 수 있다.
(이해가 안가서 알고있는 개념으로 정리하면 인간을 만들기위해 포유류라는걸 만드는느낌, express는 인간까지만 만들수 있었다)

다른 라이브러리는 코드만으로 API를 다른 개발자들한테 노출할 수 없었지만, NestJs는 노출하는게 가능해진다.
또한 NestJs 프레임워크에서 지원하는것 뿐 아니라 다른 라이브러리도 가져다가 쓸 수 있다.

# 철학

NodeJs덕분에 JavaScript를 사용하는 웹 개발자는 대부분 NodeJs라는 환경을 사용하여 소통이 가능하다.
그덕분에 Angular, React, Vue라이브러리같은 좋은 라이브러리도 탄생할수 있었다.
그러나, Node에서 나온 어떤 라이브러리도 아키텍처문제를 효과적으로 해결할수 없었다
NestJs는 테스트도 쉽고, 추가기능의 확장도 가능하며, 느슨하게 결합되고 쉽게 관리할수 있는 아키텍처를 제공한다
(아키텍처는 어떤걸 하자고 얘기한 결과물이며 만들어진 이후에도 이런식으로 보완,수정하겠다는 의미인듯)
이 아키텍처는 Angular에서 영감을받았다(기능 비슷하다는 소리인듯)

# 설치

NestJs의 처음 폴더구조를 만들려면 해당 두가지중 하나를 하면됨

```bash
npm i -g @nestjs/cli
```

를 통해 node글로벌에 설치를한다.
설치를 하면 터미널에서

```bash
nest new project-name
```

위 명령어를 통해 project-name이라는 폴더명을 가진 NestJs 프레임워크 파일을 생성할수 있다
글로벌로 설치하면 명령어가 추가되는 방식인듯

```bash
nest new --strict project-name
```

를 사용하여 프로젝트 생성시 오류를 더 명확하게 잡을수있다. 예를들어 null,undefiend 값을 참조하는걸 방지하며, 변수선언시 타입을 무조건 정의해야하는등

또는 글로벌로 설치하기 싫다면 아래명령어를 통해 git clone를 통하여 저장되어있는 파일을 내 컴퓨터로 복사할수 있다.

```bash
git clone https://github.com/nestjs/typescript-starter.git project
```

만약 TypeScript가 싫다면

```bash
git clone https://github.com/nestjs/javascript-starter.git project
```

위 명령어를통해 자바스크립트로 설치도 가능하다
만약 깃 기록없기 가져오려면 degit을 사용하면된다.

아래처럼 파일을 가져오는게 아니라 직접 만들수도 있지만, 초기설정도 직접 해줘야한다

```bash
npm i --save @nestjs/core @nestjs/common rxjs reflect-metadata
```
