오토 스케일링

- 자동으로 인스턴스를 만들어 늘렸다 줄였다함

로드밸런서

- 인스텉스가 여러개여도 하나의 주소로 접근할수 있게함
- 사용할때 반드시 대상그룹 필요함
- 로드밸런서가 대상그룹에 요청을 보낼수 있다

대상그룹

- 로드 밸런서가 어디로 보낼건지
- 대상그룹은 로드밸런서가 대상그룹으로 요청을보내면 여러개의 인스턴스에 밸런스에 맞게 분배한다
- 인스턴스가 여러개면 돈이 많이나감
- 오토 스케일로 관리
